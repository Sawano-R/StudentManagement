<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="raisetech.StudentManagement.repository.StudentRepository">

  <!-- 受講生の全件検索-->
  <select id="search" resultType="raisetech.StudentManagement.data.Student">
    SELECT * FROM students
  </select>

  <!-- 受講生の単一検索(ID)-->
  <select id="searchStudentID" resultType="raisetech.StudentManagement.data.Student">
    SELECT * FROM students WHERE id = #{id}
  </select>

  <!-- 受講生の単一検索(名前)-->
  <select id="searchStudentName" resultType="raisetech.StudentManagement.data.Student">
    SELECT * FROM students WHERE name = #{name}
  </select>

  <!-- 受講生コースの全件検索-->
  <select id="searchCourseList" resultType="raisetech.StudentManagement.data.StudentCourse">
    SELECT * FROM students_courses
  </select>

  <!-- 受講生コースの単一検索(受講生ID)-->
  <select id="searchCourseID" resultType="raisetech.StudentManagement.data.StudentCourse">
    SELECT * FROM students_courses WHERE id_students = #{idStudents}
  </select>

  <!-- コース状態の全件検索-->
  <select id="searchStatusList" resultType="raisetech.StudentManagement.data.CourseStatus">
    SELECT * FROM courses_status
  </select>

  <!-- コース状態の単一検索(受講生ID)-->
  <select id="searchStatusID" resultType="raisetech.StudentManagement.data.CourseStatus">
    SELECT * FROM courses_status WHERE id_students = #{idStudents}
  </select>

  <!-- 受講生の登録-->
  <insert id="registerStudent" parameterType="raisetech.StudentManagement.data.Student"
    useGeneratedKeys="true" keyProperty="id">
    INSERT INTO students (name, name_kana, nickname, mail, resion, age, gender, remark, deleted)
    values(#{name}, #{nameKana}, #{nickname}, #{mail}, #{resion}, #{age}, #{gender}, #{remark},
    false)
  </insert>

  <!-- 受講生コ－スの登録-->
  <insert id="registerCourse" parameterType="raisetech.StudentManagement.data.StudentCourse"
    useGeneratedKeys="true" keyProperty="id">
    INSERT INTO students_courses(id_students,course,start_day,end_day)
    values(#{idStudents}, #{course}, #{startDay}, #{endDay})
  </insert>

  <!-- コ－ス状態の登録-->
  <insert id="registerStatus" parameterType="raisetech.StudentManagement.data.CourseStatus"
    useGeneratedKeys="true" keyProperty="id">
    INSERT INTO courses_Status(id_course,id_students,status)
    values(#{idCourses}, #{idStudents}, #{status})
  </insert>

  <!-- 受講生の更新-->
  <update id="updateStudent" parameterType="raisetech.StudentManagement.data.Student">
    UPDATE students SET name=#{name}, name_kana=#{nameKana}, nickname=#{nickname}, mail=#{mail},
    resion=#{resion},
    age=#{age}, gender=#{gender}, remark=#{remark}, deleted=#{deleted} WHERE id=#{id}
  </update>

  <!-- 受講生コースの更新-->
  <update id="updateCourse" parameterType="raisetech.StudentManagement.data.StudentCourse">
    UPDATE students_courses SET course = #{course}, deleted=#{deleted} WHERE id = #{id}
  </update>

  <!-- コース状態の更新-->
  <update id="updateStatus" parameterType="raisetech.StudentManagement.data.CourseStatus">
    UPDATE courses_Status SET status = #{status} WHERE id = #{id}
  </update>
</mapper>